For matching:

Type: Date/Duration/Time
Match: Regexp # to resolve - avoiding nasty tokenisation nonsense, also POS tag indicators, where appropriate
Guard: Regexp
Squelch: True/False [default: false] # don't output a tag, and remove any tags in the subexpression
ID: some string [optional]
After: some-ID (ensure that some-ID has run first)

For normalisation:

Type: Date # type to apply to
Match: Regexp # regexp which rule applies to, groups are used in the evale
Value: Python Expression 
Mod: value # if exists, adds a mod attribute

rules are ordered by filename

To resolve:

* embedded ifs